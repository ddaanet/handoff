source_up_if_exists

# Path to the claude-plugins marketplace repo, sibling to this one.
# Used by `just release` (imported from plugin-dev/release.just) to
# bump the handoff entry in marketplace.json and push.
export MARKETPLACE_DIR="$(cd "$PWD/.." && pwd)/claude-plugins"
