## Open decisions

- Whether to run the shared `memory/MEMORY.md` compaction. The index measures ~24.5KB against a 25600-byte budget, past the point where Claude Code's loader silently drops the tail, so an entry appended at the end would be invisible. `/gitlore:index-audit` is the pass for it — measure against the loader cap, relocate, retire or merge entries, then audit the diff for lost routing. Blocked on an explicit go-ahead, since it rewrites the index every ddaanet repo loads. Four memory writes are queued behind it, all listed below.
- The version bump for the release carrying this fix. The marketplace cache is keyed by version, so a push under an unchanged version reaches no installed repo and `/plugin update` re-fetches nothing. "No bump" is not among the outcomes that deliver the fix; the call is my human partner's.

## Remaining

- Resume `/orchestrate` at Phase 1, Item 1.1, slice 3 — the action vocabulary and `edit`'s own required keys — under the write-then-mutate protocol. Then slice 4, which keeps the full four-dispatch shape.
- Close the residual recorded on Item 1.1: no test asserts that `precompact` *applies* task/todo content. A defect where `validate_*` runs for `precompact` but `apply_*` is skipped would still pass, because all 17 `precompact` payloads carry `task_clear()`/`todo_keep()`, whose correct outcome (empty manifest, no file) is indistinguishable from the gate skipping the work. Cheapest form: parametrize `test_task_write_creates_file_manifest_records_w` and `test_todo_write_creates_file_manifest_records_w` over `skill`. It belongs to slice 1's subject, the external contract's positives.
- Phases 2-4 after Item 1.1, all `inline` items: `scripts/bash-post.sh`'s stage guard and failure channel; both wrap-up SKILL.md bodies; the design record (`docs/design.md`, the changelog entry and its index line, `CLAUDE.md`).
- Close the run with the `edify:tdd-auditor` dispatch over every slice's reports, then `/deliverable-review plans/2026-09-04-checkpoint-null-no-task-file` in a fresh opus session. The auditor will meet two slices whose evidence is a mutation matrix rather than RED output; the runbook's list-revision block is what explains that.
- Use `git status --porcelain` plus `just precommit` directly rather than edify's `skills/orchestrate/scripts/verify-step.sh`: in this sandbox its clean-tree check intermittently reports a phantom tree of `$HOME`-shaped dotfiles (`.bashrc`, `.claude/agents`, ...) that do not exist on disk, and so returns DIRTY against a clean tree.
- Queued memory writes, all blocked on the index cap above: (1) the `git add` guard fact — a gitignored path on disk but never in the index exits 128 with `did not match any files`, and `git ls-files` guards that silently where `--error-unmatch` writes to stderr; (2) the sandbox phantom-`git status` observation; (3) the write-then-mutate protocol for a slice whose red was consumed by an earlier slice, and the mutation matrix as its evidence; (4) the hazard that sank a review's first pass — a mutation-restore that silently no-ops (empty `$TMPDIR` making `cp` fail) stacks the next mutation on the last, redding every row and reading as exactly the "mutations are not disjoint" critical finding the matrix exists to detect. Restore from `git checkout --`, and assert the restore.
