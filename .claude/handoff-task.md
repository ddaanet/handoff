## Current task

Publishing and release-readiness after the handoff/precompact skill restructure (memory capture inside the deciding step, commit after the checkpoint, handoff body trimmed to 1799 words): push memory with gitlore:push, then run preflight.
