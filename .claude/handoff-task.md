## Current task

The `/runbook` pass on the checkpoint-payload outline is complete:
`plans/2026-09-04-checkpoint-null-no-task-file/runbook.md` decomposes it into
five phases — payload factories over `tests/test_checkpoint.py`, the tdd
payload-layer rewrite in `scripts/checkpoint.py`, `bash-post.sh`'s stage
guard, both wrap-up skill bodies, then the design record. Reviewed by
`runbook-corrector` (one critical, four majors, twelve citation fixes, all
applied), consolidated to four slices in Item 1.1, and approved item by item
at `/proof`. The sibling `runbook-review.md` and `runbook-simplify.md` carry
the reasoning, `recall-artifact.md` the memory selection.

`/orchestrate` on that runbook is the next step, and it wants a fresh
session: planning consumed this one's budget.
