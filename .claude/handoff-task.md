## Current task

`/proof` passed `plans/2026-09-08-apply-before-mutate/runbook.md` and the
pass is committed, so the runbook is ready to execute. Next is
`/orchestrate` on it in a fresh session — the runbook's own closing note
puts orchestration in its own context budget. After that, the pass-2
minors on the deliverable review, then the release.
