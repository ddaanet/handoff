## Current task

Fix pass 1 on the deliverable review of
`plans/2026-09-04-checkpoint-null-no-task-file`. C1's remedy is settled as (b),
plan-then-apply. The `/proof` loop on
`plans/2026-09-08-apply-before-mutate/outline.md` is finished: all ten items
carry a verdict and the eight revisions are applied to the file. `/runbook` on
that outline is next, then the C1 slice, then the pass-2 minors, then the
release.
