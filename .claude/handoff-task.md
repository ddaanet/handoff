## Current task

The payload-union review's fix list is closed. What is in flight is the
residue it leaves: M3's top-level-key residual and m4's two `is_empty_body`
routes, both now ordinary fix work rather than deferred findings, and the
learnings still owed as memory facts, which no longer wait on an index
decision.
